apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-client.fullname" . }}
  labels:
    {{- include "todo-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "todo-client.selectorLabels" . | nindent 4 }}